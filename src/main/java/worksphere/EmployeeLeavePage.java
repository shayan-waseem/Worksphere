package worksphere;

import javafx.fxml.FXMLLoader;
import javafx.scene.Parent;
import javafx.scene.Scene;
import javafx.stage.Stage;
import worksphere.controller.EmployeeLeaveController;
import java.io.IOException;

public class EmployeeLeavePage {

    /*
     * public static void show(Stage stage) {
     * try {
     * FXMLLoader loader = new FXMLLoader(EmployeeLeavePage.class.getResource(
     * "/worksphere/view/EmployeeLeavePage.fxml"));
     * Parent root = loader.load();
     * 
     * EmployeeLeaveController controller = loader.getController();
     * controller.setStage(stage);
     * 
     * Scene scene = new Scene(root, 1200, 700);
     * scene.getStylesheets().add(EmployeeLeavePage.class.getResource(
     * "/worksphere/view/style.css").toExternalForm());
     * 
     * stage.setScene(scene);
     * stage.setTitle("WorkSphere - My Leave Applications");
     * stage.show();
     * } catch (IOException e) {
     * e.printStackTrace();
     * }
     * }
     */
}