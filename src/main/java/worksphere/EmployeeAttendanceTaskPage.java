package worksphere;

import javafx.fxml.FXMLLoader;
import javafx.scene.Parent;
import javafx.scene.Scene;
import javafx.stage.Stage;
import worksphere.controller.EmployeeAttendanceController;

import java.io.IOException;

public class EmployeeAttendanceTaskPage {

    /*
     * public static void show(Stage stage) {
     * try {
     * FXMLLoader loader = new
     * FXMLLoader(EmployeeAttendanceTaskPage.class.getResource(
     * "/worksphere/view/EmployeeAttendanceTaskPage.fxml"));
     * Parent root = loader.load();
     * 
     * EmployeeAttendanceController controller = loader.getController();
     * controller.setStage(stage);
     * 
     * stage.setScene(new Scene(root, 1200, 700));
     * stage.setTitle("WorkSphere - Attendance & Tasks");
     * stage.show();
     * } catch (IOException e) {
     * e.printStackTrace();
     * }
     * }
     */
}